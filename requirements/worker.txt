-r base.txt
celery[redis]==5.6.3
openai-whisper==20250625
# Parakeet ASR engine (pluggable transcription backend)
onnx-asr==0.12.0
# onnxruntime-gpu (CUDA build) is installed in docker/Dockerfile.worker: it must
# replace the CPU-only onnxruntime that fastembed pulls in, not coexist with it.
silero-vad==6.2.1
numpy==2.4.6
pandas==3.0.5
openai>=2.48.0
anthropic>=0.120.0
google-genai>=2.14.0
watchdog==6.0.0

# Document parsing. PyMuPDF covers PDF text, table detection and page
# rendering; the Office readers cover the formats whose content is structured
# XML rather than pixels, which is why no headless-office renderer is needed
# here. python-docx is also pinned in backend.txt (Markdown-to-DOCX export);
# the worker needs its own copy because the two images share no layer.
PyMuPDF==1.28.0
python-pptx==1.0.2
python-docx==1.2.0
openpyxl==3.1.5
# Thin wrapper over the tesseract binary installed in docker/Dockerfile.worker.
# Pure Python with no compiled dependencies, so it cannot disturb the
# onnxruntime-gpu pin the way an ONNX-based OCR package would.
pytesseract==0.3.13

# pyannote.audio dependencies (installed manually to avoid torch downgrade)
asteroid-filterbanks==0.4.0
einops==0.8.2
huggingface_hub==1.24.0
lightning==2.6.5
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
pyannote-core==6.0.1
pyannote-database==6.1.1
pyannote-metrics==4.1
pyannote-pipeline==4.0.0
pytorch-metric-learning==2.9.0
rich==15.0.0
safetensors==0.8.0
torch-audiomentations==0.12.0
torchmetrics==1.9.0
soundfile==0.14.0
matplotlib==3.11.1
pyannoteai-sdk==0.4.0
torchcodec==0.11.1
fastembed==0.8.0
