"""Per-user subscription-CLI OAuth credential service.

Storage and (later milestones) the device-code auth flow and conversation
manager for routing inference through a user's Claude subscription via the
Claude Agent SDK. At M1 only the encrypted credential store exists.
"""
